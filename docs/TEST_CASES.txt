TEST CASES — DocuSync
=====================
Research Title: HYBRID PEER-TO-PEER DOCUMENT SYNCHRONIZATION SYSTEM WITH EVENT SOURCING AND CONFLICT RESOLUTION
Lead Researcher: Palamara, Paul John G.


SECTION 1: SIMPLE TEST CASES
==============================

TC ID   | Test Scenario / Objective              | Test Steps                                                        | Expected Result                                                                   | Status
------- | -------------------------------------- | ----------------------------------------------------------------- | --------------------------------------------------------------------------------- | ------
STC-01  | Welcome page displays on first launch  | 1. Launch DocuSync Desktop.<br>2. Observe initial screen.         | Welcome page shows logo, feature cards, and "Enter Workspace" button.             | PASSED
STC-02  | Request a new account                  | 1. Click "Request Access" on login page.<br>2. Enter email. Click "Submit Request". | Confirmation: "Request Sent! Your profile request for [email] has been logged locally." | PASSED
STC-03  | Admin approves pending account         | 1. Log in as Admin on Web.<br>2. Go to Admin Dashboard. Click "Approve". | User status changes to "Active". A PIN is generated. User can now log in.         | PASSED
STC-04  | Log in with valid credentials          | 1. Enter registered email and 6-digit PIN.<br>2. Click "Unlock Vault". | User is authenticated and redirected to the Peers page.                           | PASSED
STC-05  | Create a collaboration room            | 1. Go to Peers page. Click "Create Room".<br>2. Enter room name. Confirm. | Room is created. A 6-character OTP code is displayed with tap-to-copy.            | PASSED
STC-06  | Join a room via OTP code               | 1. Click "Join Room" on Peers page.<br>2. Enter OTP code. Click "Join". | OTP is validated via Redis. User joins the room and sees the Files workspace.     | PASSED
STC-07  | Share a file to the room               | 1. Click "Share File to Room" on Files page.<br>2. Select a .txt, .md, .csv, or .json file. | File appears in room workspace with name, size, and type icon. Peers can see it.  | PASSED
STC-08  | Open file in TipTap editor             | 1. Click a file on the Files page.                                 | Editor opens with content, formatting toolbar, and footer metadata.               | PASSED
STC-09  | Edit and observe auto-save             | 1. Type new content in the editor.<br>2. Watch footer status.      | Auto-saves after 500ms. Footer shows delta size and peers notified count.         | PASSED
STC-10  | Real-time sync between two peers       | 1. Open same file on Desktop and Web.<br>2. Type on Device A. Watch Device B. | Edits from Device A appear on Device B. "Synced from peer" banner appears.      | PASSED
STC-11  | Apply text formatting                  | 1. Select text in editor.<br>2. Click Bold, Italic, or Heading.    | Text is formatted. Toolbar button highlights. Formatting persists across saves.   | PASSED
STC-12  | View file edit history                 | 1. Open a previously edited file.<br>2. Click History icon in toolbar. | History page shows timeline of events with type, node ID, and timestamp.         | PASSED
STC-13  | Restore a previous version             | 1. Go to History page. Click "Restore" on an earlier version.       | Editor reverts to selected version. A "restore" event is logged.                  | PASSED
STC-14  | View connected peers                   | 1. Join a room with at least one peer.<br>2. Check Peers page or Right Panel. | Peers page shows node IDs, online status, and connection info.                   | PASSED
STC-15  | View Metrics dashboard                 | 1. Navigate to Metrics page.                                       | Gauges show Sync Latency, Conflict Rate, Peer Count, EventLog size. Updates live. | PASSED
STC-16  | Toggle dark/light theme                | 1. Go to Settings. Click theme toggle.                             | UI switches theme. Preference persists across sessions.                           | PASSED
STC-17  | View supported/rejected file types     | 1. Go to Settings. Scroll to File Types.                           | Shows supported (.txt, .md, .csv, .json) and rejected (.png, .zip, .exe) types.   | PASSED
STC-18  | View Node ID and config                | 1. Go to Settings. Check Node Configuration.                       | Shows Node ID, sync port (9000), and algorithm details.                           | PASSED
STC-19  | Admin generates a new account          | 1. As Admin, click "Provision New Account".                        | Node ID and temp PIN are generated and displayed on screen.                       | PASSED
STC-20  | Admin views users and rooms            | 1. Go to Admin Dashboard.<br>2. Check "Registered Users" and "Active Repos". | Tables show all users (Node ID, status) and rooms (name, OTP, host, members).   | PASSED
STC-21  | Admin deletes a group by OTP           | 1. Enter OTP in "Delete Repository" field.<br>2. Click "Terminate Group". Confirm. | Confirmation dialog appears. Room is removed from Active Repositories.           | PASSED (Revised)
STC-22  | Leave a room                           | 1. Click "Leave Room" on Files page.<br>2. Confirm.                | Confirmation modal appears. User returns to Peers page.                           | PASSED
STC-23  | Download a file from editor            | 1. Open a file. Click "Download".                                  | File downloads with original filename and extension (e.g., notes.txt).             | PASSED
STC-24  | Delete a file from the room            | 1. Click Trash icon on file row.<br>2. Confirm deletion.           | File is removed. A "delete" event is logged in EventLog.                           | PASSED
STC-25  | User logs out                          | 1. Click Logout from sidebar or Settings.                          | Session cleared. Returns to Vault Login.                                          | PASSED


SECTION 2: EDGE TEST CASES
============================

TC ID   | Test Scenario / Objective              | Test Steps                                                        | Expected Result                                                                   | Status
------- | -------------------------------------- | ----------------------------------------------------------------- | --------------------------------------------------------------------------------- | ------
ETC-01  | Login with incorrect PIN               | 1. Enter valid email, wrong PIN.<br>2. Click "Unlock Vault".      | Login rejected. Error: "Invalid login". PIN input shakes.                         | PASSED
ETC-02  | Login with unregistered email          | 1. Enter unregistered email and any PIN.<br>2. Click "Unlock Vault". | Login rejected. Error: account not found.                                      | PASSED
ETC-03  | Join room with invalid OTP             | 1. Enter random 6-character code.<br>2. Click "Join".              | "Room not found. Check the invite code and try again."                            | PASSED
ETC-04  | Non-admin accesses Admin page          | 1. Log in as regular user.<br>2. Navigate to Admin route.          | Shows "Admin access restricted" with lock icon and "Back to Files" button.        | PASSED
ETC-05  | Concurrent edits trigger LWW           | 1. Open same file on Desktop and Web.<br>2. Type different text simultaneously.<br>3. Wait for sync. | If clocks are resolvable, LWW auto-resolves with Toast: "Conflict resolved using Last-Write-Wins." If edits are genuinely concurrent (neither dominates), the edit is escalated to the Conflicts page for manual review. | PASSED
ETC-06  | View and resolve conflict manually     | 1. After ETC-05, go to Conflicts page.<br>2. Review diff. Click Accept or Reject. | Side-by-side diff with word-level highlights (yellow = local offline edit). Resolving updates the file and closes the conflict. | PASSED
ETC-07  | Edit file while offline                | 1. Disconnect from network. Edit a file.<br>2. Reconnect to network. | Edits save locally. On reconnect, pending edits sync to peers or escalate to Conflicts if the remote version diverged. | PASSED
ETC-08  | Request account with existing email    | 1. Enter already-registered email.<br>2. Click "Submit Request".   | "This profile is already registered." No duplicate created.                       | PASSED
ETC-09  | Admin deletes group with empty OTP     | 1. Leave OTP field empty.<br>2. Try clicking "Terminate Group".    | "Terminate Group" button stays disabled. No API call is made.                     | PASSED (Revised)
ETC-10  | Open rejected binary file              | 1. Open .png or .zip file in editor.                              | Editor shows informational message that binary files are not supported.           | PASSED
ETC-11  | Verify delta sends minimal data        | 1. Edit a large file. Add one word.<br>2. Check delta size in footer. | Footer delta (e.g., "42 B") is much smaller than total file size.                 | PASSED
ETC-12  | Copy OTP via tap-to-copy               | 1. Create room. Click OTP display.<br>2. Paste to verify.          | OTP copied to clipboard. Button shows "Copied!" with green check for 2 seconds.   | PASSED
ETC-13  | Revoked user auto-logged out           | 1. Admin revokes a user's access.<br>2. Wait a few seconds on user's device. | Revoked user is auto-logged out within 2 seconds. Returns to Vault Login.         | PASSED
ETC-14  | Metrics on fresh system                | 1. Open Metrics page with no prior activity.                      | Shows "No data yet" or "0" gracefully. No NaN or errors.                           | PASSED


SECTION 3: COMPLEX TEST CASES
================================

TC ID   | Test Scenario / Objective              | Test Steps                                                        | Expected Result                                                                   | Status
------- | -------------------------------------- | ----------------------------------------------------------------- | --------------------------------------------------------------------------------- | ------
CTC-01  | End-to-end: Login → Room → Share → Edit → Sync | 1. Log in on Desktop. Create room.<br>2. Join room from Web using OTP.<br>3. Share a file. Edit on both devices. | Both devices see same content. Edits sync in real-time. EventLog records all events with correct vector clocks. | PASSED
CTC-02  | Three-device sync (Desktop + Web + Mobile) | 1. Create room. Join from Web and Mobile.<br>2. Edit file on each device sequentially.<br>3. Compare content across all three. | All three devices show identical content. Peers page shows 3 peers. Metrics reflect increased activity (Desktop/Web only). | PASSED
CTC-03  | Conflict: detect → auto-resolve → manual review | 1. Open file on Desktop and Web.<br>2. Go offline. Edit same paragraph on both.<br>3. Reconnect. Go to Conflicts page.<br>4. Accept preferred version. | Genuinely concurrent offline edits are escalated to manual review. Conflicts page shows side-by-side diff with yellow-highlighted offline edits. Accepting resolves it and logs a conflict-resolve event. | PASSED
CTC-04  | Admin lifecycle: Generate → Login → Revoke → Auto-logout | 1. Admin provisions account.<br>2. User logs in with generated credentials.<br>3. Admin revokes access. | New user logs in successfully. Admin sees user as "Active". After revocation, user is auto-logged out within 2 seconds. | PASSED
CTC-05  | History restore verification           | 1. Make 5 edits to a file.<br>2. Go to History. Restore version #2.<br>3. Verify content matches version #2. | History shows 5 entries. Restoring version #2 reverts content. A "restore" event is appended to the log. | PASSED
CTC-06  | Cross-platform file type handling      | 1. Share .txt, .md, .csv, .json, .png files.<br>2. Open each on Desktop and Web.<br>3. Verify consistent behavior. | Supported types open correctly on both platforms. .png shows "not supported" message. Behavior is identical. | PASSED
CTC-07  | Offline edit with delayed merge        | 1. Both devices editing a file.<br>2. Disconnect Desktop. Edit on both.<br>3. Reconnect. Observe merge. | Offline edits save locally. On reconnect, changes merge or escalate. Conflicts (if any) appear on Conflicts page for manual review. | PASSED
CTC-08  | Metrics accuracy under load            | 1. Create room with 2 devices.<br>2. Edit file back and forth 10 times.<br>3. Check Metrics page. | Gauges show accurate Sync Latency, Peer Count (2), Conflict Rate, and EventLog Size. Charts update in real-time. | PASSED
CTC-09  | Room lifecycle: Create → Join → Collaborate → Leave → Terminate | 1. Create room. Join from Web.<br>2. Edit file. Web leaves room.<br>3. Admin terminates group via OTP. | Room shows 2 members. After leave, user returns to Peers. After termination, room disappears from listings. | PASSED
CTC-10  | Delta encoding efficiency              | 1. Open 1000-word file.<br>2. Add 1 word. Note delta size.<br>3. Add 50 words. Note delta size. | 1-word delta << 50-word delta << total file size. Proves Myers diff sends only changed bytes, not full document. | PASSED
CTC-11  | Vector clock ordering across peers     | 1. Device A edits 3 times. Device B edits 2x.<br>2. Check History page timestamps. | History shows events in causal order. Each node's counter increments correctly.  | PASSED
CTC-12  | Admin Dashboard cross-platform         | 1. Log in as Admin on Desktop and Web.<br>2. Compare data. Delete group on one.<br>3. Verify change on the other. | Both dashboards show same users, rooms, and audit logs. Action on one reflects on the other within 10 seconds. | PASSED
CTC-13  | Multiple files, independent editing    | 1. Share 3 files (.txt, .md, .json) to room.<br>2. Edit file A on Desktop, file B on Web.<br>3. Verify file C remains unchanged. | Each file has independent history. Editing file A does not affect B or C.          | PASSED
CTC-14  | Theme persistence across pages         | 1. Switch to light theme. Navigate all pages.<br>2. Close and reopen app. | Theme persists across pages and after app restart. No flashing or reverting.        | PASSED
CTC-15  | Full audit trail: login to logout      | 1. Admin generates account. User logs in.<br>2. Create room, share file, edit, conflict.<br>3. Log out. Admin checks audit log. | Session Audit Log contains timestamped entries for each action (LOGIN, EDIT, SYNC, CONFLICT_RESOLVE, etc.). | PASSED


REVISION LOG
============
1. ETC-15 ("Run algorithm demo in Settings") removed — feature was never part of the production system.
2. STC-02 expected result updated to match the system's actual confirmation message.
3. ETC-03 expected result updated to match the system's actual error message.
4. ETC-08 expected result updated to match the system's actual error message.
5. ETC-05 and CTC-03 expected results clarified to distinguish auto-resolved (LWW dominance) vs. escalated (genuinely concurrent) conflicts, matching the manual-review design confirmed in the system's conflict resolution engine.
